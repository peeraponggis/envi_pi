# envi_pi — Memory สำหรับ Claude Code

ระบบนำเสนอข้อมูลผลกระทบด้านสิ่งแวดล้อมประเทศไทย ดึงจากหน่วยราชการ · repo `https://github.com/peeraponggis/envi_pi`
แผนฉบับเต็ม: `docs/แผนระบบ.md` · แหล่งข้อมูล: `docs/แหล่งข้อมูลราชการ.md` · ติดตั้ง: `docs/ขั้นตอนติดตั้ง Supabase.md`

## สถาปัตยกรรม (ตัดสินใจแล้ว 3 ก.ย. 2569 — อย่าเปลี่ยนโดยไม่ถามผู้ใช้)
- **โปรเจกต์ Supabase แยกต่างหาก** จาก `pi-boq-dev` ของ CRM — ไม่ใช้ profiles/ตาราง/FK ร่วมกัน · ใช้ schema `public`
- หน้าเว็บ **plain HTML + CDN ES module ไม่มี build** อยู่ใน `web/` · ไคลเอนต์ Supabase **ตัวเดียว** (cache Promise ใน `envi-core.js`)
- ดึงข้อมูลราชการด้วย **pg_cron → Edge Function `envi-ingest`** (วางโค้ดใน Dashboard editor เพราะเครื่องไม่มี CLI)
  สำเนาหลักอยู่ `supabase/functions/envi-ingest/index.ts` — แก้ที่นี่ก่อนเสมอ แล้วเพิ่ม `VERSION`
- **PostGIS** เปิดใน schema `extensions` → SQL ที่แตะ geom ต้อง `extensions.ST_xxx` หรือ `set search_path = public, extensions`
- **anon อ่านข้อมูลราชการได้** (เว็บสาธารณะ) · ล็อกอินเพื่อบันทึกผล/นำเข้าไฟล์ · เปลี่ยนได้โดยลบบล็อก anon ใน `20260903000200_rls.sql`
- API ต่างประเทศ (NASA POWER, PVGIS, Nominatim, GIBS) เรียกสดจากเบราว์เซอร์ + แคช `api_cache` · API ไทยผ่าน DB เท่านั้น (HTTP-only/CORS)
- Secrets (ENVI_CRON_TOKEN, TMD_UID/UKEY, GISTDA_API_KEY, FIRMS_MAP_KEY) อยู่ใน Edge Secrets + Vault เท่านั้น **ห้ามเขียนลงไฟล์**
- `web/js/config.js` มีแค่ URL + publishable key (เปิดเผยได้) — ค่าเริ่มต้นเป็น placeholder ผู้ใช้กรอกเอง

## เผยแพร่ออนไลน์ (5 ก.ย. 2569 ผู้ใช้เปิด GitHub Pages เอง)
- **GitHub Pages** จาก branch `main` โฟลเดอร์ `/ (root)` → `https://peeraponggis.github.io/envi_pi/web/index.html` · `…/web/wastewater.html` · `…/web/water-realtime.html` · push ไป main แล้วขึ้นเว็บเองใน 1–2 นาที (ไม่มี build) · repo public อยู่แล้ว ไม่มี secret ในไฟล์ · หน้าเว็บอ่าน Supabase ด้วย publishable key ตามการออกแบบ
- ทดสอบในเครื่อง: `node scripts/serve.mjs` → `http://127.0.0.1:5510/web/…`

## โครงไฟล์
- `supabase/migrations/…_schema.sql` (1) · `…_rls.sql` (2) · `…_rpc.sql` (3) · `…_seed_sources.sql` (4) · `…_cron.sql` (5, รันหลังตั้ง Vault) · `…_import.sql` (6)
- `supabase/เปิดสิทธิ์ Data API.sql` (รันหลัง 1-3) · `supabase/ตรวจสภาพ.sql` (แสดงทุกแถว raise เฉพาะเรื่องใช้งานไม่ได้)
- `web/js/envi-core.js` ไม่แตะ DOM (ทดสอบ `node --test test/*.test.mjs`) · `web/js/app.js` ที่เดียวที่แตะ DOM/Cesium · `web/js/import.js` หน้า admin
- `web/css/pi.css` คัดลอกจาก pi_crm_erp (ธีมเขียว Sarabun) · `web/css/envi.css` override เฉพาะแผนที่ — ห้ามนิยามสีใหม่นอกตัวแปร
- ไฟล์ต้นฉบับ `solar energy potential based on satellite data V2.9.0.html` เก็บไว้เป็นอ้างอิง ไม่แก้

## กติกา SQL (ยกมาจาก pi_crm_erp)
1. รันซ้ำได้ทุกไฟล์ · 2. ทุกตารางเปิด RLS · 3. สคริปต์ grant ห้ามครอบ begin/commit ดัก error รายตาราง `raise exception` ตอนท้าย
4. revoke execute จาก **public** ก่อน grant ฟังก์ชัน (anon ได้สิทธิ์ผ่าน PUBLIC) · 5. ตรวจผลต้องแสดงทุกแถว ห้ามกรองด้วยค่าที่อาจไม่มี
6. งานหลายขยักรายงานว่าขยักไหนผ่าน (import_* คืน inserted/skipped/errors)

## กติกาข้อมูลราชการ
- ค่าเป็น string + sentinel (-1, -999, "", "-") → `num()` คืน null ห้ามเก็บ -999 ลง `value`
- วันที่ พ.ศ. → ลบ 543 เมื่อปี > 2400 · เวลาไทยไม่มี TZ → ต่อ +07:00 (`parseThaiDate` / `thaiDateTimeToISO`)
- ห้ามใช้ `new Date().toISOString().slice(0,10)` เป็นวันนี้ — ใช้ `todayISO()`
- ArcGIS 1000 แถว/คำขอ วน `resultOffset` · DGR 117k แถว ใช้ `ingest_runs.cursor`
- handler ต้อง fail-soft: บันทึก `ingest_runs.error` ไม่ throw ให้ cron ค้าง
- LDD landuse = CC BY-NC-ND ห้าม simplify · HII = CC BY-NC · แสดงเครดิตทุกแหล่ง (แถบล่างขวาแผนที่อ่านจาก `sources.agency`)
- endpoint ราชการส่วนใหญ่ **ไม่มีเอกสาร** (Air4Thai, GISTDA PM2.5, Royal Rain) — เปลี่ยนได้ทุกเมื่อ อย่าเชื่อโครงสร้างจากความจำ ให้ดู `ingest_runs.error`

## สถานะ (3 ก.ย. 2569 — ติดตั้งจริงแล้ว)
- ✅ โปรเจกต์ Supabase `envi-pi` ref `mplexdeaqgrdoqqhypqb` (Org ใหม่ `envi-pi` แผน Free, บัญชี Gmail แยกของผู้ใช้) — SQL ทั้ง 7 ไฟล์รันผ่าน · Vault มี `envi_ingest_url` + `envi_cron_token`
- ✅ Edge Function `envi-ingest` v1.0.1 deploy แล้ว (Verify JWT ปิด · Secret `ENVI_CRON_TOKEN` ตั้งแล้ว) · cron 11 job · `cron_enabled=true` 7 แหล่ง: air4thai, tmd_quake, gistda_hotspot_modis/viirs, gistda_pm25, royalrain_radar, dgr_wells
- ✅ ข้อมูลจริงเข้าแล้ว: สถานี Air4Thai 173 · hotspot 1,533 · แผ่นดินไหว 10 · PM2.5 ดาวเทียม 77 จังหวัด · เรดาร์ 9 · **บ่อบาดาล DGR ครบรอบแล้ว 107,973 บ่อ** (4 ก.ย. 69 07:50 จาก api_total 117,634 — ที่เหลือไม่มีพิกัด/พิกัดนอกประเทศ) แล้ววนรอบใหม่ทุกชั่วโมง
- ✅ Edge **v1.2.1** (3 ก.ย. 69 ค่ำ): `upsert()` ตัดแถวที่คีย์ onConflict ซ้ำในชุดเดียว (แถวหลังชนะ) — DGR หน้า 8 มี `no` ซ้ำ ทำ cron dgr_wells ล้ม "ON CONFLICT DO UPDATE command cannot affect row a second time" ทุกชั่วโมงตั้งแต่ 14:50 · ตรวจ cron หลังลดขนาด DB: ทุก job succeeded, ทุกแหล่ง ok
- ✅ ผู้ใช้ admin: `wisemenow9@gmail.com` (สร้าง 3 ก.ย. 69)
- ✅ เฟส 2-3 นำเข้าแล้ว (3 ก.ย. 69, ผ่าน RPC import_* ด้วย session admin ในเบราว์เซอร์): ป่าสงวนแห่งชาติ 1,221 แปลง (layer `rfd_reserve_forest`, simplify 15 m) · ดินถล่ม DMR 56,177 จุด (events, `YEAR_DATA` ว่างทั้งหมด → occurred_at = 1970-01-01) · ศูนย์กลางตำบล 7,364 (stations `dopa_tambon`)
  ไฟล์ดิบ+GeoJSON อยู่ `data/raw/` (gitignore) · SHP ทั้งสองเป็น UTM zone 47 WGS84 มาใน .rar (แตกด้วย WinRAR ที่ `C:\Program Files\WinRAR\UnRAR.exe`)
- ✅ **DWR ArcGIS REST** (`https://gis.dwr.go.th/arcgis/rest/services` เปิดสาธารณะ 27 ชั้น — พบจากหน้า webgis.dwr.go.th/projects ที่เปิด web map ของ ArcGIS Enterprise) ดึงด้วย `scripts/fetch_arcgis.mjs` แล้วนำเข้าแล้ว (3 ก.ย. 69 บ่าย):
  ลุ่มน้ำหลัก 28 (`dwr_main_basin`) · ลุ่มน้ำสาขา 359 (`dwr_sub_basin`) · จังหวัด 77 (`dwr_province`) · อำเภอ 883 (`dwr_amphoe`) · แรมซาร์ 113 (`onep_ramsar`) · เขื่อน 1,087 (stations `dwr_dam`) · สถานีอุทกวิทยา 126 (`dwr_hyd_station`)
  ชั้นที่ยังไม่ดึง: Tambon 7,640 โพลิกอน · WELL 72,658 · NAT_WTR_BODY 28,394 · 25_BASIN · พื้นที่ชุ่มน้ำท้องถิ่น 36,408 จุด · ประสิทธิภาพการระบายน้ำ (เส้น)
- ✅ **โพลิกอนเก็บเป็นชิ้นย่อย** (migration 1000 แทน 700): `layer_features` เก็บชิ้นจาก `ST_Subdivide(…,256)` โดยตรง — 1 ฟีเจอร์ต้นฉบับ = หลายแถวที่ `ext_id/name_th/props` ซ้ำกัน (`insert_layer_feature()` ทำให้เอง) · ไม่มี `layer_features_idx` แล้ว · `site_report.layer_hits` ใช้ `distinct on (layer_id, ext_id, name_th)` · `layer_features_bbox` ส่งชิ้น (เส้นแบ่งชิ้นเห็นบนแผนที่ ยอมรับได้) · ที่มา: ก่อนมีชิ้นย่อย โพลิกอนลุ่มน้ำหลัก/แรมซาร์ทำให้ 57014 statement timeout
- ⚠️ **ขนาด DB Free 500 MB** (3 ก.ย. 69 ค่ำ): หลัง EIA/โรงงาน DB ขึ้น 537 MB → Dashboard "EXCEEDING USAGE LIMITS" · ผู้ใช้เลือกลดขนาดแทนอัป Pro → migration 1000 (เลิกเก็บโพลิกอนสองสำเนา + ตัด props โรงงานที่ซ้ำ) + `vacuum full` 3 ตาราง → **326 MB** (layer_features 182 · events 104 · stations 19) · ตรวจ site_report 3 จุดผลเท่าเดิม
  กติกา: ก่อนนำเข้าชั้นใหม่ให้ `select pg_size_pretty(pg_database_size(current_database()))` ก่อน · **อย่านำเข้า LDD landuse** (รายจังหวัด 3–130 MB ห้าม simplify) จนกว่าจะอัป Pro หรือย้ายชั้นใหญ่ไป Storage bucket/PMTiles · observations hourly เก็บ 180 วันจะโตราว 1–2 MB/วัน ถ้าใกล้เพดานให้ลดเป็น 90 วันใน `purge_old_observations`
- ✅ **เฟส 4 สภาพอากาศ (3 ก.ย. 69 เย็น)**: TMD **NWP API** (JWT ผู้ใช้สมัครเอง หมดอายุ ก.ย. 2570) เก็บเป็น Secret `TMD_NWP_TOKEN` · Edge `envi-ingest` v1.1.0 มี action สาธารณะ `{"action":"forecast","lat","lng"}` (ไม่ต้องมี cron token, CORS เปิด) แคช 1 ชม./พิกัดปัด 2 ตำแหน่งใน `api_cache` · แท็บสภาพอากาศแสดง 24 ชม. + 7 วัน
  ⚠️ ผู้ใช้ **ไม่ได้สมัคร TMD API ชุดเก่า (uid/ukey)** — handler `tmd_today`/`tmd_3h` (สถานีตรวจวัดจริง 122 สถานี) ยังรันไม่ได้ ถ้าต้องการต้องสมัครแยกที่ data.tmd.go.th/api
  ⚠️ token JWT ของ TMD ถูกวางในแชตตอนส่งให้ — ถ้ากังวลให้ revoke แล้วออกใหม่ที่ data.tmd.go.th/nwpapi แล้วอัปเดต Secret
- ✅ **เฟส 5 GISTDA api-gateway (3 ก.ย. 69 ค่ำ)**: Secret `GISTDA_API_KEY` (key ส่งเป็น `?api_key=` หรือ header `API-Key`) · Edge v1.2.0
  - handler `gistda_flood` (cron ทุก 6 ชม. เปิดแล้ว): STAC `flood/collections/flood1day_r2/items` → assets.data GeoJSON → RPC `replace_layer_features('gistda_flood_1d')` (migration 800, service_role เท่านั้น) · วันไม่มีน้ำท่วม = 0 โพลิกอน · flood3/7/30day ไม่มี item · gi-service flood-recurrence 404 ทุกเวอร์ชัน
  - action สาธารณะ `burnscar` {lat,lng}: `resources/features/burn-scar?bbox=±0.045°` (79,435 โพลิกอนทั้งประเทศ ไม่ควรนำเข้า) แคช 24 ชม. → แท็บภัยพิบัติแสดงจำนวน/ไร่/ชนิดที่ดิน + วาดโพลิกอนสีส้มด้วย GeoJsonDataSource
  - WMS flood-freq มี (Vallaris) แต่ต้องแนบ key ใน URL → ไม่ซ้อนในเบราว์เซอร์ (key จะหลุด) ถ้าจะใช้ต้อง proxy tile ผ่าน Edge
  - 🐛 แก้ไปด้วย: cron job เรดาร์เรียก `'royalrain'` แต่ id จริง `'royalrain_radar'` (ไม่เคยยิง) · คอมเมนต์ที่มี slash-star-slash ทำ bundler ของ Supabase พังตอน deploy
- ✅ ตำบลโพลิกอน DWR `dwr_tambon` 7,639/7,640 (3 ก.ย. 69) — แปลงที่ข้ามคือเศษ 4 จุดของ ต.สวาย อ.ปรางค์กู่ ศรีสะเกษ (TAMBON_IDN 330709 มี 2 ชิ้น ชิ้นจริงเข้าแล้ว) · TAMBON_IDN ซ้ำได้ (หลายชิ้นต่อตำบล) จึงมี ext_id ซ้ำ 170 แถว — ปกติ · ที่ตั้งในรายงานอ่านจากชั้นนี้ก่อน (ต./อ./จ.) แล้วค่อย Nominatim
- ✅ **พพ. แผนที่รังสีอาทิตย์** (3 ก.ย. 69): ชุด `sed01` ที่แคตตาล็อกบอกว่า "38 สถานี" จริง ๆ เป็น XLSX **รายตำบล 7,416 จุด พ.ศ. 2560** (จังหวัด/อำเภอ/ตำบล + lat/lon + 12 เดือน หน่วย MJ/m²/วัน ค่า 15–20) → stations `dede_solar` station_type `solar_grid`, meta {monthly[12], annual_avg_mj, annual_kwh_m2 (= MJ/3.6×365)} · แท็บแสงอาทิตย์แสดงตำบลใกล้สุด (nearest_stations 15 กม.) ก่อน NASA POWER — ที่ปทุมธานี พพ. 1,827 vs NASA 1,852 kWh/m²/ปี สอดคล้องกัน
  ⚠️ `nearest_stations()` ไม่คืน `meta` — ฝั่งเว็บต้อง `fetchStationMeta(station_id)` เพิ่ม (ถ้าจะแก้ให้คืน meta ต้องแก้ไฟล์ 3 แล้วรันซ้ำ)
- ✅ **โรงงาน กรมโรงงานฯ** (3 ก.ย. 69): CSV factype3 (จำพวก 3 ทั้งประเทศ 67,695, 39 MB) + factype2 (3,220) + fac101-105-106 (จัดการของเสีย 2,940) จาก data.go.th → events kind=other source `diw_factory` 68,116 แห่ง · **ไม่มีพิกัดในไฟล์** → ผูกกับศูนย์กลางตำบล `dopa_tambon` (ตำบล 97.7% / อำเภอ 2.3%) จึงเป็นความแม่นระดับตำบล · props: type_code/type_name/class/hp/workers/waste_handler · site_report `factories_5km` (migration 900) · gdcatalog.go.th ถูก Incapsula กัน curl — ใช้ data.go.th แทน
- ✅ **EIA (สผ. Smart EIA)** (3 ก.ย. 69 ค่ำ): ไม่มี API → `scripts/scrape_eia.mjs` (list = POST /site/eia 100 แถว/หน้า มี CSRF · detail = GET /eia/detail?id= ~45 นาที 4 พร้อมกัน) → `scripts/build_eia_rows.mjs` → events source `onep_eia` **13,350 โครงการ** (จาก 13,510: ไม่มีที่ตั้ง 160) พิกัด ตำบล 84% / อำเภอ 11% / จังหวัด 4% · props: category/subcategory/report_type/status/project_status/owner/consultant/location/approval_date · occurred_at = วันที่แจ้งเห็นชอบ · site_report `eia_20km` (มาบตาพุด 1,027 โครงการ, ปทุมธานี 566)
  ⚠️ บั๊กที่เจอ: parser ที่ตั้งรุ่นแรกใช้ lookahead กว้างจน "จังหวัด…" ถูกตัดทิ้ง — ตอนนี้ตัดค่าที่ป้ายถัดไปที่รู้จักเท่านั้น · ถ้าจะอัปเดตทะเบียน: รัน list ใหม่ แล้ว detail จะดึงเฉพาะ id ที่ยังไม่มีใน eia_detail.json
- ✅ **ระบบบำบัดน้ำเสีย คพ. DSPOT เข้าฐานจริง** (DSPOT = **Database System for Publicly Owned Treatment Works** ฐานข้อมูลระบบบำบัดน้ำเสียรวมชุมชน คพ. พัฒนาร่วมกับ GIZ · ผู้ใช้ขอให้แสดงชื่อเต็มในหน้าเว็บ 6 ก.ย. 69) (5 ก.ย. 69 ค่ำ): migration 1100 (sources `pcd_dspot`/`bma_wwtp`/`onep_pap`, `site_report.wwtp_10km` = ใกล้สุด 5 แห่งใน 10 กม., cron `envi_pcd_dspot` อังคาร 03:20 น.) + Edge **v1.3.0** handler `pcd_dspot`: ดึง HTML `https://dspot.pcd.go.th/database/s?area=` → parse `<script id="__NEXT_DATA__">` → `props.pageProps.reports[]` (อย่าใช้ `/_next/data/<buildId>/` เพราะ buildId เปลี่ยนทุก deploy · ไม่มี CORS) → stations `pcd_dspot` station_type `wwtp` **216 แห่ง** (meta: status/plant_type/operator/capacity/inflow_pct/pop/basin/discharge/wq{bod,tss,ph,tn,tp}/fee/photo) + observations 1,237 แถว (bod_in/out, tss, ph, inflow_pct, avg_inflow ณ `date_quality`)
  **กทม. 7 โรง (`bma_wwtp`) + สผ. 92 ระบบ (`onep_pap`) เข้า stations แล้ว** (5 ก.ย. 69 ค่ำ) ผ่าน `supabase/seed/wastewater_stations.sql` ที่ `scripts/build_wastewater_data.mjs` สร้าง (upsert ตาม source_id, ext_id รันซ้ำได้) · สผ. อีก 14 ระบบไม่มีที่ตั้งพอระบุพิกัดจึงไม่เข้า · หน้าเว็บ `web/wastewater.html` (Leaflet SPA แยกจาก index.html) อ่าน stations ทั้ง 3 source จาก Supabase ก่อน → fallback สแนปช็อต `web/data/wastewater.json` · พื้นแผนที่ ESRI ถนน/ดาวเทียม/ภูมิประเทศ + Google แผนที่/ดาวเทียม (tile URL `mt{0-3}.google.com/vt` ไม่มี key — ใช้ภายใน/ต้นแบบ ถ้าเผยแพร่สาธารณะควรใช้ Maps API key ตาม TOS) · มือถือ ≤900px: แผนที่บน + แผงแท็บ สรุป/รายการ/ที่มา ล่าง, ตัวกรองซ่อนหลังปุ่ม
  อจน. (wma.or.th, เดินระบบ 59 แห่ง) ไม่มี API สาธารณะ · ชุดรายจังหวัดบน gdcatalog เป็นสถิติรวม
- ⚠️ ขนาด DB **437 MB** (5 ก.ย. 69 ค่ำ) — โตจาก 326 เพราะบ่อบาดาล DGR ครบ 107,981 แถว · เหลือราว 60 MB ก่อนเพดาน Free · observations โต 1–2 MB/วัน → ควรลด `purge_old_observations` เป็น 90 วัน หรือ `vacuum full public.stations` หลังรอบ DGR (upsert ซ้ำทุกชั่วโมงทำ bloat)
  สำรวจ 5 ก.ย. 69: layer_features 183 MB (66,539 ชิ้น = 8 ชั้น ป่าสงวน 1,221 / ตำบล 7,485 / แรมซาร์ 113 / อำเภอ 882 / ลุ่มน้ำสาขา 359 / ลุ่มน้ำหลัก 28 / จังหวัด 77 / น้ำท่วม 0) · stations 113 MB (124,550 แถว 11 source; dgr_wells 107,981 · dede_solar 7,416 · dopa_tambon 7,364 · dwr_dam 1,087 · pcd_dspot 216 · air4thai 173 · dwr_hyd_station 126 · onep_pap 92 · gistda_pm25 77 · royalrain_radar 11 · bma_wwtp 7) · events 113 MB (139,200 แถว: diw_factory 68,116 · dmr_landslide 56,177 · onep_eia 13,350 · hotspot viirs 1,001 + modis 532 · tmd_quake 24) · observations 7 MB (61k hourly + 1,236 daily + 2,534 latest) · อื่น ๆ < 1 MB
  🐛 พบระหว่างสำรวจ: events hotspot ล่าสุด `occurred_at` = **2023-04-06** ทั้ง MODIS/VIIRS — GISTDA hotspot_daily MapServer อาจคืนชุดเก่า หรือ handler แปลงวันที่ผิด ต้องตรวจ `ingest_runs` + ค่า `ACQ_DATE` ของฟีเจอร์จริง
- ✅ **เซนเซอร์น้ำเรียลไทม์เข้าฐานแล้ว (5 ก.ย. 69 ค่ำ)** migration 1200 + Edge **v1.4.0**:
  - **กรมโรงงานฯ POMS** handler `diw_poms` (cron ทุก 15 นาที): `factory-ws/get/factory-list?page=N` → stations `diw_poms` station_type `factory_sensor` **796 โรงงาน** (800 − 4 พิกัดใช้ไม่ได้) · `get/measurement-list/{id}` แบ่ง 200 โรงงาน/รอบ ด้วย `cursor.offset` (ครบทุกชั่วโมง, concurrency 8) · parameter = `"<code>:<param>"` เช่น `P0078:COD` · **observations เก็บเฉพาะจุดวัดน้ำ (typeName ≠ CEMS)** ส่วน CEMS (ปล่องอากาศ) เก็บใน latest_observations อย่างเดียว extra {type, code, name, severity} · ไม่ต้องล็อกอิน แต่ไม่มี CORS และ endpoint ประวัติต้องล็อกอิน
  - **คพ. IWIS** handler `pcd_iwis` (cron นาที 10 ทุกชั่วโมง): `api-iwis.pcd.go.th/mst-station-with-summary?limit=200` (5.6 MB) → stations `pcd_iwis` station_type `river_auto` 64 สถานี · observations เฉพาะรอบนาที :00 จาก `water_quality_summary_50` (พารามิเตอร์ PH/DO/EC/Temp/Salinity/Tur/BOD/COD/NH4N) · latest ทุกพารามิเตอร์ + extra {level, color} ของ คพ.
  - `site_report` เพิ่ม `poms_5km` (nearest_stations 5 กม. 10 แห่ง) และ `iwis_20km` (3 แห่ง) — คืน latest jsonb ให้ด้วย · แดชบอร์ด `web/wastewater.html` ชั้น "โรงงานมีเซนเซอร์น้ำทิ้ง (POMS)" ◆ สีแดงเมื่อ severity ≥ 2 และ "สถานีคุณภาพน้ำแม่น้ำ (IWIS)" ▲ สีตามระดับ DO อ่านจาก `v_station_latest`
  - ⚠️ **เปลี่ยนอายุข้อมูลรายชั่วโมงเป็น 7 วัน** (`envi_purge_observations` ทุกวัน 19:00 UTC `purge_old_observations(7)`; เดิม 180 วัน/สัปดาห์) เพราะ Free เหลือ ~60 MB และแถวรายชั่วโมงรวม ≈ 57k แถว/วัน ≈ 7 MB/วัน · ประวัติยาวอยู่ใน `observations_daily` (rollup ทุกคืน ไม่ถูกลบ) · ถ้าอัป Pro ค่อยขยาย
  - **แดชบอร์ดคุณภาพน้ำเรียลไทม์ `web/water-realtime.html`** (5 ก.ย. 69 ดึก ตามรูปตัวอย่างของผู้ใช้): โดนัทแหล่งน้ำตามระดับ DO (สถานีแย่สุดต่อแหล่งน้ำ), รายชื่อแหล่งน้ำต่อระดับ, ฟีดแจ้งเตือน (DO < 4, pH นอก 5.5–9, POMS severity ≥ 2), แท่งซ้อน+ตารางรายภาค, โดนัท/ตารางจังหวัด/Top COD ของ POMS, กราฟย้อนหลังรายสถานี (Chart.js 4.4.1 จาก cdnjs; จุดสีตามระดับ DO ≥6/4/2 · BOD ≤20 · COD ≤120; โหมดรายชั่วโมงจาก `observations` 7 วัน / รายวันจาก `observations_daily`) · เกณฑ์ระดับ DO ตามภาพของ คพ. ประกาศเป็นตัวแปร `--lv-*` ในหน้านี้ · anon อ่าน observations/observations_daily ได้อยู่แล้ว
  - **กราฟย้อนหลังทุกแหล่ง** (ในหน้าเดียวกัน, 5 ก.ย. 69 ดึก ตามที่ผู้ใช้ขอ): เลือกแหล่ง (IWIS/POMS/DSPOT/Air4Thai/GISTDA PM2.5 = ทุก source ที่มี observations) → สถานี → พารามิเตอร์ → ขอบเขต **รายสถานี / จังหวัดเดียวกัน / ภาคเดียวกัน** (radio; หลายเส้น สูงสุด 40 สถานี, query `observations … in(station_id) like '%:COD'` สำหรับ POMS) · เส้นประ = เกณฑ์อ้างอิงจาก `PARAM_INFO` (DO 6/4/2, pH 5.5–9, BOD/COD/TSS น้ำทิ้ง 20/120/30, PM2.5 37.5/75, AQI 100/200 …) · คำอธิบาย+มาตรฐานใต้กราฟ, สรุป 5 ระดับ (นับทุกจุดที่แสดง), ตารางรายสถานี min/avg/max/ล่าสุด · ภาคของสถานีอากาศ/โรงงานได้จากตาราง `REGION_OF` (77 จังหวัด) ในหน้า · ระดับ effluent vs surface แยกด้วย `infoKey(param, kind)` (`eff:BOD`, `eff:COD`)
  - migration 1300: `purge_old_observations` ลบเฉพาะแหล่งที่ `refresh_minutes` 1–1440 → ผลแล็บ DSPOT (สัปดาห์ละครั้ง, ลงวันที่ตรวจปี 2567–68) ไม่ถูก purge
  - ✅ 6 ก.ย. 69 เช้า: migration 1300 รันแล้ว (purge 02:00 ลบผลแล็บ DSPOT ไปก่อน → `trigger_ingest('pcd_dspot')` เติมกลับทันที 166 ระบบ) · Edge **v1.4.1** deploy แล้ว (ผู้ใช้กดเองใน Chrome เพราะแผง Browser ของแอปยังรายงาน hidden) ตรวจจาก endpoint จริง 09:52
  - **ข้อมูลเครื่องมือวัด (ผู้ใช้ขอ 5 ก.ย. 69)**: ต้นทาง **ไม่มีแหล่งใดเปิดยี่ห้อ/รุ่นหัววัด** — POMS `get/instrument/{id}` ตอบ 403 ต้องล็อกอิน มีแค่ `measName` (ชื่อจุดวัด เช่น "จุดที่ 1", "PC Boiler") + `treatmentSystemDetail` (ชนิดระบบบำบัด เช่น AS, DAF, Chemical treatment) · IWIS `mst_station.LOGGER/PROJECT_NAME/STATION_TYPE_NAME` ว่างทั้ง 64 สถานี · Air4Thai มีแค่ `stationType` (GROUND/BKK/MOBILE) → หน้าเว็บแสดง "เครื่องมือวัด" ใต้พารามิเตอร์ด้วยข้อมูลเท่าที่มี + ระบุชัดว่าต้นทางไม่เปิดยี่ห้อ (`sensorText()` ใน water-realtime.html, popup POMS/IWIS ใน wastewater.html)
  - แดชบอร์ดเพิ่ม (5 ก.ย. 69 ดึก): ภาพรวมอากาศ Air4Thai (โดนัท PM2.5 4 ระดับ, ตารางรายภาค, 10 สถานีฝุ่นสูงสุด คลิกดูกราฟ) · ลองซ้ำ 3 ครั้งเมื่อ Supabase ตอบ 401 "JWT issued at future"/statement timeout (เหตุขัดข้องฝั่ง Supabase 2–5 ก.ย.) + ป้ายแจ้งแทนหน้าว่าง · index.html มีลิงก์ไปสองแดชบอร์ด
  - ผลรอบแรก 5 ก.ย. 69 22:00–22:10: POMS 796 โรงงาน (สไลซ์แรก 199 มีค่า, น้ำ 137) · IWIS 64 สถานี ค่าล่าสุด 59 สถานี ระดับ DO: ดี 20 / พอใช้ 14 / เสื่อมโทรม 12 / เสื่อมโทรมมาก 5 / ไม่มีค่า 13 (ตัวอย่าง สะพานโยทะกา นครนายก DO 0.96 mg/L)
  - ระบบบำบัดชุมชน 315 แห่งใน DB **ไม่มีเซนเซอร์** มีแค่ผลแล็บปีละครั้ง (DSPOT 159 ระบบ) · Dashboard ของ Supabase โหลดช้ามาก/ค้างช่วงมีเหตุขัดข้อง (status.supabase.com "API Gateway degraded") — ยิง handler ครั้งแรกจึงปล่อยให้ cron ทำแทน `trigger_ingest` ด้วยมือ
- ✅ **หน้าส่งออกรายงาน `web/report.html`** (6 ก.ย. 69 ผู้ใช้ขอ "การ์ดเลือกรายงานตามต้องการ"): 3 ขั้น เลือกการ์ด (7 ใบ: IWIS · POMS · DSPOT · Air4Thai · รายงานจุดที่เลือก (site_report) · ประวัติรายสถานี · สรุปผู้บริหาร) → ตั้งค่า (ช่วง ค่าล่าสุด/24 ชม./7 วัน/รายวัน · ขอบเขต ทั้งประเทศ/ภาค/จังหวัด · เรียง · ส่วนประกอบ KPI/กราฟ/ตาราง/เฉพาะเกินเกณฑ์/เกณฑ์ · หัวรายงาน) → พรีวิว A4 → พิมพ์ PDF (`window.print` + `@media print`) · CSV · **XLSX** (SheetJS 0.18.5 จาก cdnjs ชีตต่อตาราง + ชีตสรุป) · ลิงก์แชร์ `?cards=…&range=…&scope=…` เปิดแล้วพรีวิวเอง
  กราฟใช้ Chart.js `responsive:false` ขนาดคงที่ (660×210) ให้พิมพ์ไม่เพี้ยน · สถิติช่วงเวลา (min/avg/max) ดึง observations `in(station_id)` จำกัด 8,000 แถว หรือ observations_daily · **เกณฑ์/คำอธิบาย/ระดับ/ชื่อระบบบำบัด/ภาค ย้ายไป `web/js/param-info.js`** (export LV, PARAM_INFO, infoKey, levelOf, pcdLevel, PLANT_TYPE, regionOf) ใช้ร่วมกับ water-realtime.html — แก้เกณฑ์ที่เดียว
- ✅ **ระบบออกแบบเซนเซอร์และพลังงาน/โซลาร์** (6 ก.ย. 69 ตามแผนใน plan file "แผนเฟสใหม่"): `web/js/sensor-catalog.js` (โมดูลบริสุทธิ์ ทดสอบ `test/sensor-catalog.test.mjs` 9 เทสต์) — `PROCESS_TYPES` 11 ระบบ (AS/OD/SBR/AL/SP/CW/RBC/MBR/UASB/Anaerobic/IND) แต่ละระบบมี stages, จุดวัด (required/target/note), benchmark `kwh_m3` (estimate+source), พารามิเตอร์จำลอง · `SENSOR_TYPES` 16 ชนิด (tech, W, comms, มาตรฐาน, ราคาช่วง, เกณฑ์เลือก, บำรุงรักษา) · `BRAND_EXAMPLES` 35 รุ่น (Hach, E+H, Xylem/YSI, Horiba, Yokogawa, s::can, Siemens, ABB, VEGA, Schneider, Advantech, Teltonika …) ติดป้าย "ตัวอย่างข้อมูลสาธารณะ ไม่ใช่การรับรอง" · `EXTRA_PARAM_INFO` (MLSS, ORP, Level, in:BOD/COD/TSS) · ฟังก์ชัน `designSystem`, `powerBudget`, `plantEnergy`, `measuredIntensity` (POMS Watt÷Flow), `solarSizing` (kWp = kWh/วัน×margin ÷ (PSH×PR), PR = 0.8×(1−0.004×(Tmod−25)) King/Sandia), `batterySizing`, `pshFromDede`, `makeSeries` (mulberry32 seed, sin รายวัน+noise+event), `summarizeSeries`, `payloadFor` (Modbus→MQTT→observations row), `designToURL/FromURL`
  หน้า `web/sensor-design.html`: เลือกระบบ/ความสามารถ/โรงจริง DSPOT (เติม plant_type, capacity, avg_inflow, พิกัด) → ผังกระบวนการ+BOQ (qty แก้ได้) → พลังงาน 3 ชุด (เซนเซอร์ / โรงบำบัด / รวม) + โซลาร์ kWp/พื้นที่/ลงทุน/คืนทุน + แบตสำรอง (PSH: กรอกเอง → พพ. dede_solar 15 กม. → NASA POWER → 4.8) + POMS วัดจริง (โรงงาน) → จำลองสายส่ง 5 โหนดพร้อม payload + กราฟ 7 วันจุดสีตามระดับ + สรุปแจ้งเตือน → XLSX / เปิดในรายงาน / ลิงก์ / บันทึก (ต้องล็อกอิน) · การ์ด `sensor` ใน report.html (อ่าน `?cards=sensor&design=<qs>`)
  ✅ migration 1400 `sensor_designs` รันแล้ว (ผู้ใช้รันเอง 6 ก.ย. 69 บ่าย) — ตรวจผ่าน REST: anon select/rpc ถูกปฏิเสธ 42501 ตามออกแบบ · บันทึก/โหลด/ลบผ่าน RPC `save_sensor_design` + RLS เจ้าของ (ทดสอบด้วยบัญชี admin ในหน้า index.html) · ทุกตัวเลขพลังงาน/ราคา/PR เป็นค่าประมาณ ติดป้าย `.est`
- ✅ **โหมดสาธารณะซ่อนชื่อโรงงาน POMS** (6 ก.ย. 69 ผู้ใช้กังวลข้อกฎหมายจากการติดป้ายเชิงลบคู่ชื่อบริษัท): `config.PUBLIC_FACTORY_NAMES=false` → ผู้ไม่ล็อกอินเห็น "โรงงาน <รหัส> · <ประเภท>" (`envi-core.factoryLabel`, `canSeeFactoryNames` = สาธารณะ ‖ มี session) ในทุกจุด: แดชบอร์ด (โรงงาน COD สูงสุด, แจ้งเตือน, ตัวเลือกสถานี/กราฟ), แผนที่ (รายการ+แถบข้อมูล ซ่อนทะเบียน/ที่อยู่ด้วย), รายงาน (ตาราง POMS, สรุปผู้บริหาร, รายงานจุด, ประวัติ) · ถ้อยคำ POMS เป็นกลาง `param-info.POMS_LEVEL_NAME` ("อยู่ในค่าอ้างอิง/สูงกว่าค่าอ้างอิง…") ใช้คำ "เกินเกณฑ์" เฉพาะเมื่อ POMS แจ้งเอง (severity ≥ 2) + `POMS_DISCLAIMER` (ค่า ณ เวลา ไม่ใช่ผลตรวจราชการ ที่มา) · เกณฑ์เสื่อมโทรมของ คพ. ยังใช้กับ IWIS/DSPOT ตามเดิม
- ✅ แผนที่ระบบบำบัด: popup → **แถบข้อมูลด้านขวา** `#info` (พับ ▾/▸, ปิด ×, ลากย้ายด้วยหัวแถบ pointer events, จำตำแหน่ง localStorage `envi_info_pos`, ดับเบิลคลิกหัว = กลับที่เดิม, มือถือเป็นแผ่นล่างเต็มกว้าง) — คลิกหมุด/รายการเรียก `showInfo(r)` ใช้ HTML จาก `popup(r)` เดิม
- ✅ **แก้ผังกระบวนการ/ชนิดระบบให้ตรงหลักวิชาการ** (8 ก.ย. 69 ผู้ใช้ทัก): อ้างอิง Metcalf & Eddy 5th ed. + คู่มือระบบบำบัดน้ำเสียชุมชน คพ.
  - `PROCESS_TYPES` เพิ่ม `recycles[]` (**สายวนกลับแยกจากลำดับการไหลของน้ำ**) — AS/OD/MBR/IND มี RAS+WAS · SBR/RBC มีแต่ WAS (ไม่มี RAS: SBR ตกตะกอนในถังเดียว, RBC เป็นฟิล์มติดที่) · AL/SP/CW ไม่มีสายวนกลับเลย · UASB/Anaerobic มีสายก๊าซชีวภาพ · `sludge` ออกจาก `stages` แล้ว (เป็นปลายทางของ WAS ไม่ใช่ปลายทางของน้ำ) ทุกระบบจบที่ `outlet`
  - แก้ลำดับหน่วย: ทุกระบบมี `screen`+`grit` แยกกัน · **SP เป็นชุดสามบ่อ** แอนแอโรบิก→แฟคัลเททีฟ→บ่อบ่ม (เดิมรวมเป็นบ่อเดียว) · **AL จบด้วยบ่อตกตะกอน** ไม่ใช่บ่อบ่ม · **MBR เพิ่มตะแกรงละเอียด 1–3 มม.** และไม่มี clarifier/disinfect (เมมเบรน 0.04–0.4 µm กรองแบคทีเรีย) · **OD ไม่มี primary** (เติมอากาศยืดเวลา) · **UASB/Anaerobic เพิ่ม `post`** (ลำพังไม่ผ่านมาตรฐาน) · **IND เพิ่ม `neutral`** (ปรับกรด-ด่าง/เคมี)
  - จุดวัดใหม่: `SBlanket` ชั้นตะกอน · `Air` อัตราอากาศโบลเวอร์ · `Cl2` คลอรีนคงเหลือ · `TMP` ความดันคร่อมเมมเบรน · `NO3N` · `Biogas`/`CH4` · Flow บนสาย RAS/WAS (แม่เหล็กไฟฟ้า) · RBC วัดกำลังไฟมอเตอร์เพลาแทนภาระบิด · SBR น้ำออกใช้ magflow ไม่ใช่รางเปิด (ระบายเป็นกะ) · `SENSOR_TYPES` +6 ชนิด · `pointName(stage,param)` ตั้งชื่อจุดวัดตามตำแหน่ง (Flow ที่ inlet/ras/was/membrane/outlet ไม่ใช่ "น้ำทิ้ง" ทั้งหมด) · `m.sensor` override ได้
  - แก้ชื่อไทยใน `PLANT_TYPE`: UASB "ไร้อากาศแบบตะกอนลอย"→"ไหลขึ้นผ่านชั้นตะกอนจุลินทรีย์" · AL "สระเติมอากาศ"→"บ่อเติมอากาศ" · SBR "แบบกะ"→"แบบเป็นกะ" · RBC/MBR/DAF ปรับให้ตรงศัพท์
  - หน้าเว็บ: `sensor-design.html` แยกแถวผังน้ำกับแถวสายวนกลับ (เส้นประ + "จาก → ถึง" + อัตราออกแบบ) พร้อมคำอธิบายกระบวนการและเกณฑ์ออกแบบ · `report.html` การ์ด sensor แสดงสายวนกลับ/เกณฑ์ออกแบบด้วย · เทสต์ 30 ตัว (เพิ่ม 2 ชุดตรวจความถูกต้องเชิงวิชาการ)
- ✅ **ผังตามแบบ คพ. + แผนที่โรงจริง + ข้อเสนอโซลาร์/การเงิน** (8–9 ก.ย. 69 ผู้ใช้ทักว่าผังของทั้ง 11 ระบบเกือบเหมือนกันหมด และ "ค่าประมาณ" ไม่บอกวิธีคิด)
  - **แหล่งอ้างอิงหลัก**: คู่มือระบบบำบัดน้ำเสียชุมชน กรมควบคุมมลพิษ 2563 `https://www.pcd.go.th/publication/4241/` (ดึงข้อความเต็มแล้ว ตารางเกณฑ์ 5-1/5-3/5-5/5-7/5-8/5-10) · ประกาศมาตรฐานน้ำทิ้งชุมชน `https://www.pcd.go.th/laws/4508` (ดึง PDF แล้วอ่านด้วย `pdftotext -enc UTF-8`) · ยังไม่ได้: *เกณฑ์แนะนำการออกแบบฯ คพ. 2563* `https://www.pcd.go.th/publication/6380/` ไฟล์ 23.9 MB ดึงอัตโนมัติไม่ได้
  - **คู่มือ คพ. จัดประเภทระบบชุมชนไว้ 5 ประเภท** (SP AL AS CW RBC) โดย OD และ SBR เป็นรูปแบบย่อยของ AS · สำมะโน พ.ย. 2559: SP 45 · AS 37 · AL 17 · CW 1 · RBC 1 · โปรแกรมคงรหัส DSPOT 8 ตัวไว้ แต่ติดธง `pcd_type` ว่าประเภทไหนคู่มือรับรอง · **UASB/Anaerobic/MBR/IND เป็นระบบอุตสาหกรรม ไม่ใช่ระบบชุมชน** คู่มือระบุว่าน้ำเสียชุมชนไทยเข้มข้นไม่พอสำหรับระบบไร้อากาศ
  - **ผังแก้ใหม่ทั้งหมด**: ทุกระบบมีบ่อสูบน้ำเสีย+ตะแกรงดักขยะ · SP เป็นชุดบ่อ 4 แบบ (แอนแอโรบิค แฟคัลเททีฟ แอโรบิค บ่อบ่ม) **ไม่มีบ่อเติมคลอรีน** ตามที่คู่มือระบุ · AL จบด้วยบ่อบ่ม + **บ่อเติมคลอรีน 1 บ่อ** ไม่มีสูบสลัดจ์กลับ · **AS ชุมชนไทยไม่มีถังตกตะกอนขั้นต้น** สูบสลัดจ์กลับ 0.25–1 · OD และ RBC มี**บ่อปรับสภาพการไหล** OD ใช้**รางดักกรวดทราย** สูบกลับ 0.75–1.5 · SBR ไม่มีถังตกตะกอนและสายสูบกลับ มีบ่อพักน้ำทิ้ง · CW แบ่ง 3 ส่วนตามชนิดพืช · เพิ่มหน่วยจัดการตะกอน (บ่อสูบตะกอน บ่อพักตะกอน เครื่องรีดตะกอน ลานตากตะกอน) ซึ่ง**คู่มือ คพ. ไม่มีบทว่าด้วยเรื่องนี้** จึงอ้างจากเอกสารโรงจริง
  - ⚠️ **แก้มาตรฐานน้ำทิ้งที่อ้างผิดฉบับ**: ประกาศ ทส. 7 เม.ย. 2553 (ราชกิจจานุเบกษา เล่ม 127 ตอนพิเศษ 69 ง) มีเพียง **6 พารามิเตอร์** — pH 5.5–9.0 · บีโอดี ≤ 20 · ของแข็งแขวนลอย ≤ 30 (บ่อปรับเสถียร/บ่อผึ่ง ≤ 50 และใช้บีโอดีของน้ำกรอง) · น้ำมันและไขมัน ≤ 5 · **ไนโตรเจนทั้งหมด ≤ 20** · **ฟอสฟอรัสทั้งหมด ≤ 2** — **ไม่มีซีโอดีและไม่มีทีเคเอ็น** ค่าเหล่านั้นเป็นเกณฑ์ของน้ำทิ้งจากอาคาร/โรงงาน คนละประกาศ · แก้ที่ `param-info.js` แล้ว เพิ่ม `eff:TN`, `eff:TP`, `eff:FOG` และเซนเซอร์ที่รองรับ · บ่อปรับเสถียรได้เกณฑ์ผ่อนปรนเฉพาะเมื่อเก็บกัก ≥ 7 วันโดยคิดปริมาตรที่ความลึกไม่เกิน 2 เมตร
  - ⚠️ **ไม่มีตาราง kWh/m³ ของราชการไทย** ตัวเลขเดียวที่เป็นทางการคือกำลังกวนบ่อเติมอากาศ ≥ 0.525 kW ต่อ 100 ลบ.ม. → `energy.source` ของทุกระบบระบุชัดว่า "ไม่ใช่ตัวเลขที่ราชการไทยประกาศ" (มีเทสต์บังคับ)
  - ✅ **ผังเป็นภาพวาด SVG** `schematicSVG(design)` ในแคตตาล็อก (คืนสตริงล้วน ทดสอบได้) รูปทรง 21 แบบตามของจริง ใช้ทั้งหน้าออกแบบและรายงานพิมพ์ · เส้นทางน้ำทึบ สายสลัดจ์ประส้ม ก๊าซประแดง ขุดลอกเส้นจุด · หมุดเซนเซอร์บนหน่วยที่ติดตั้ง · สีอ้าง CSS variable ของ pi.css · เทสต์ยืนยันว่าลำดับรูปทรงของ 11 ประเภทไม่ซ้ำกัน
  - ✅ **กรอบแผนที่ดาวเทียม** ในการ์ดสรุปด้านขวา (Leaflet 1.9.4 · ESRI Imagery/Google Hybrid/ESRI Street · ซูมได้) — **กล่องแผนที่ต้องอยู่นอก `#out` และย้ายเข้าออกด้วย `parkMap()`/`syncMap()` ทุกครั้งที่คำนวณใหม่** เพราะการ์ดถูกเขียนทับด้วย `innerHTML` ทั้งก้อน (เจอบั๊กนี้จริงตอนทำ)
  - ✅ **โมดูลการเงิน** ใน `sensor-catalog.js`: `billEnergy` (บิลค่าไฟจริง ติดป้าย "วัดจริง" ไม่ใช่ค่าประมาณ) · `plantEnergyResolved` (บิล > น้ำเข้าจริง > ความสามารถออกแบบ + คิด kWh/m³ ย้อนกลับเทียบช่วงอ้างอิง) · `capexPerKwp` (ราคาตามขนาด 5 ชั้น × ตัวคูณวิธีติดตั้ง หลังคา/พื้นดิน/ทุ่นลอยน้ำ) · `batteryBankSizing` · `solarOption` · `cashflow` 25 ปี · `npv` `irr` (แบ่งครึ่งช่วง คืน `null` ไม่ใช่ NaN) `paybackYears` `lcoe` `simpleRoi` · `epcOffer` `ppaOffer` `epcVsPpa` · **ทุกผลลัพธ์แนบ `steps[]` ที่มีสูตรพร้อมตัวเลขที่แทนแล้ว** (กติกา: ห้ามมี `<` `>` ใน formula เพราะตารางถอดแท็กด้วย regex — มีเทสต์บังคับ)
  - **PPA ส่วนลดค่าไฟ 20% เป็นคนละตัวกับอัตราคิดลด NPV 7%** (ผู้ใช้ยืนยัน 8 ก.ย. 69) · ค่าอ้างอิงราคาทั้งหมดเป็นตลาดเชิงพาณิชย์ 2569 ไม่ใช่ราคากลางราชการ · ตัวคูณทุ่นลอยน้ำเป็นค่าอ้างอิงสากล ไม่ใช่ตัวเลขไทย (ระบุใน `MOUNTS.floating.source` และมีเทสต์)
  - ⚠️ **คีย์ตัวเลขใหม่ทุกตัวต้องใส่ใน `NUM_KEYS`** ของ `designFromURL` ไม่งั้น `report.html` ได้สตริงแล้วคำนวณเพี้ยนเงียบ ๆ · ตัวเลือกวิธีติดตั้งเก็บเป็นสตริงคั่นจุลภาคเพราะ `designToURL` ข้ามออบเจกต์
- ✅ **เกณฑ์ออกแบบตามประกาศกรมควบคุมมลพิษ** (9 ก.ย. 69 ดาวน์โหลดเอกสาร 23.9 MB ที่เคยดึงอัตโนมัติไม่ได้สำเร็จ ด้วย `curl` แล้วอ่านด้วย `pdftotext -enc UTF-8 -layout`)
  - **ที่มาที่แข็งที่สุดที่มี**: `https://www.pcd.go.th/laws/4932/` → **ประกาศกรมควบคุมมลพิษ เรื่อง เกณฑ์การออกแบบระบบรวบรวมน้ำเสียและระบบบำบัดน้ำเสียรวมของชุมชน ประกาศ ณ วันที่ 5 มี.ค. 2553 · ราชกิจจานุเบกษา เล่ม 127 ตอนพิเศษ 72 ง หน้า 27 · 8 มิ.ย. 2553** — เนื้อหาเดียวกับ `https://www.pcd.go.th/publication/6380/` (เล่ม 1 คู่มือ คพ. ร่วมกับ วสท. 2546) แต่เป็นประกาศราชการ จึงอ้างฉบับนี้ · ไฟล์ที่โหลดเก็บไว้ที่ scratchpad ไม่ได้เข้ารีโป
  - **ข้อค้นพบหลัก**: เอกสารทำจากการสำรวจโรงจริงทั่วประเทศ และระบุว่า **น้ำเสียชุมชนไทยมีบีโอดีต่ำกว่าค่าอ้างอิงต่างประเทศมาก ระบบในอดีตจึงถูกออกแบบใหญ่เกินจริง** · แนะนำเพียง 3 กระบวนการสำหรับชุมชนไทย: บ่อปรับเสถียร · **สระเติมอากาศแบบผสมบางส่วน** · **เอเอสแบบเติมอากาศยืดเวลา** (ไม่ใช่แบบธรรมดา) · ใช้กับชุมชนระดับเมืองเท่านั้น ห้ามใช้กับน้ำเสียอุตสาหกรรม อาคารสูง หรือหมู่บ้าน
  - **ตารางที่ 2.2 ลักษณะน้ำเสียชุมชนไทย** (แก้ `in:BOD`/`in:TSS` ที่เดิมอ้างตำราต่างประเทศ): ท่อระบายรวม บีโอดี 65–110 (แนะนำ 80) · ของแข็งแขวนลอย 40–110 (80) · ทีเคเอ็น 10–40 (30) · ฟอสฟอรัส 1–6 (4) · ท่อระบายแยก แนะนำ 160/160
  - **ตารางที่ 7.4** เติมอากาศยืดเวลา: อายุสลัดจ์ 20–30 วัน · F/M 0.05–0.15 · MLSS 3,000–6,000 · Qr/Q 0.5–1.0 · ออกซิเจน 1.4–1.6 ก.O₂/ก.BOD · เก็บกักไม่น้อยกว่า 6 ชม. · เอสบีอาร์ MLSS 1,500–3,000
  - **ตารางที่ 7.1** บ่อแฟคัลเททีฟ: ภาระบีโอดีเชิงพื้นที่ 20–25 ก./ตร.ม.-วัน ที่ 25 °C · ลึกไม่น้อยกว่า 1.5 ม. · **แนะนำแฟคัลเททีฟอนุกรม 2 บ่อ ไม่แนะนำบ่อแอนแอโรบิกสำหรับน้ำเสียชุมชน** · **ตารางที่ 7.2** สระเติมอากาศ เก็บกัก 1–2 วัน ลึก 2–4 ม. ออกซิเจน 0.7–1.0 กำลังกวน **1.5–3.0 kW ต่อ 1,000 ลบ.ม.** + บ่อขัดแต่ง 1–2 วัน
  - **ตารางที่ 7.5 SAE ของเครื่องเติมอากาศ** (กก.O₂/kWh): หัวฟูรูพรุน 1.9–6.6 · ไม่ใช่รูพรุน 1.3–1.9 · ดูดพ่น 2.2–3.5 · ผิวน้ำหมุนช้า 1.5–2.1 · หมุนเร็ว 1.1–1.4 · หมุนแนวนอน 1.5–2.1 · แบบดูด 0.5–0.8 · กังหันจมน้ำ 1.1–2.1
  - **`aerationEnergy(code, {...})`** คิดหน่วยไฟเครื่องเติมอากาศจากเกณฑ์ไทยโดยตรง สองทางแล้วเลือกทางที่มากกว่าตามที่ประกาศระบุ (ความต้องการออกซิเจน กับ กำลังกวน) → ผลออกมา **ต่ำกว่าช่วงอ้างอิงสากล 3–8 เท่า** ซึ่งตรงกับข้อสังเกตของเอกสารเรื่องออกแบบใหญ่เกินจริง · ⚠️ เป็นเฉพาะไฟเครื่องเติมอากาศ ไม่รวมสถานีสูบน้ำเสีย ปั๊มสลัดจ์ และระบบสลัดจ์ · ตัวปรับประสิทธิภาพในสนาม 0.55 เป็นข้อสมมติของเราเอง ไม่ได้อยู่ในประกาศ
  - **รูปที่ 9.1 สายสลัดจ์ที่เหมาะกับไทย**: ทำข้นสลัดจ์ (ถังทำข้นด้วยแรงโน้มถ่วง) → แยกน้ำ (ลานตากแห้ง/สายพานรีด/เครื่องอัดกรอง/หมุนเหวี่ยง) → ถังพักกากตะกอน → กำจัด (หมักทำปุ๋ย/ฝังกลบ) · เติมอากาศยืดเวลาให้สลัดจ์เสถียรอยู่แล้ว **ไม่ต้องมีบำบัดขั้นต้นและไม่ต้องปรับเสถียรสลัดจ์**
  - ✅ **ได้ครบทั้ง 3 เล่มแล้ว** (9 ก.ย. 69): `www.pcd.go.th` เผยแพร่เฉพาะเล่ม 1 · เล่ม 2 กับ 3 อยู่ที่คลังห้องสมุด **มทส.** `http://sutlib2.sut.ac.th/sut_contents/H92223.{1,2,3}.pdf` — **ต้องส่ง header `Referer: http://sutlib2.sut.ac.th/` ไม่งั้นได้ 403** · เล่ม 1 2.0 MB · **เล่ม 2 รายละเอียดสนับสนุนเกณฑ์แนะนำการออกแบบ 17.1 MB** (10 บท ขยายความเล่ม 1) · **เล่ม 3 ตัวอย่างการคำนวณ 6.0 MB** (บทที่ 2 กระบวนการเอเอส · บทที่ 3 กระบวนการสระเติมอากาศ · ภาคผนวก ก ตัวอย่างแบบองค์ประกอบระบบรวบรวมน้ำเสีย · ภาคผนวก ข มาตรฐานงานโครงสร้างเสริมเหล็ก) · ทั้งสามเล่มมีชั้นข้อความ แตกด้วย `pdftotext` ได้ · มิเรอร์สำรองเป็นหนังสืออิเล็กทรอนิกส์ที่ FlipHTML5 (`fliphtml5.com/wfrt/sciw|tvzc|ocee/basic`) แต่ไม่มี PDF ให้ดาวน์โหลดตรง
  - ✅ **`aerationEnergy()` ทวนตัวอย่างการคำนวณของ คพ. ได้ตรงทุกบรรทัด** (เล่ม 3 หัวข้อ 3.7 สระเติมอากาศ 20,000 ลบ.ม./วัน แบ่ง 3 สระ): บีโอดีเข้า 80 → ออก 16 · กำจัด 428.8 กก./วัน = 18 กก.O₂/ชม. · SAE ในสนาม 1.8 × 0.5 = 0.9 · ทางออกซิเจน 20 kW · ปริมาตร 13,400 ลบ.ม. · ทางกวนผสม 34 kW · **เลือกทางกวนผสมเพราะมากกว่า** → ทั้งโรง 0.121 kWh/m³ · มีเทสต์ล็อกค่าเหล่านี้ไว้ · จากตัวอย่างนี้จึงแก้ค่าตั้งต้น: **ตัวปรับในสนาม 0.5** (เป็นค่าทางการ ไม่ใช่ข้อสมมติของเราแล้ว) · เวลาเก็บกักใช้ค่าสูงสุดของช่วง · กำลังกวน 2.5 kW/1,000 ลบ.ม. · ความต้องการออกซิเจนใช้ค่าสูงสุดของช่วงตามที่ตัวอย่างเลือก (`o2_mode: 'typical'` ให้ใช้ค่ากลางเมื่อต้องการประเมินการใช้ไฟจริงแทนการออกแบบ)
- ✅ **ปรับหน้าออกแบบตามที่ผู้ใช้ทัก** (9 ก.ย. 69)
  - **ตรวจการพิมพ์ PDF** (9 ก.ย. 69): เนื้อหายาวเกินหนึ่งแผ่นจริงและมาก — การ์ดแบบระบบกับข้อเสนอราคาการ์ดละ 4 แผ่น A4 · การ์ด POMS 351 โรงงานยาว 31 แผ่น · ตัวหนักคือตาราง BOQ 526 มม. และตารางวิธีคิด 478 มม. · **ไม่ได้แก้ด้วยการตัดเนื้อหา** แต่แก้ให้ตัดหน้าถูกที่: `thead { display: table-header-group }` ทำให้หัวตารางซ้ำทุกแผ่น · `tr`, `.chartbox`, `canvas`, `.kpi`, `.box`, `.note`, `.crit`, `.sch-print` ตั้ง `page-break-inside: avoid` ห้ามตัดกลาง · `h3 { page-break-after: avoid }` กันหัวข้อค้างท้ายแผ่น · ตรวจแล้วไม่มีบล็อกห้ามตัดใดสูงเกินพื้นที่พิมพ์ 269 มม. (297 − ขอบบน 12 − ขอบล่าง 16) จึงไม่มีการแตกซ้อนหรือช่องว่างใหญ่ · **ท้ายกระดาษเดิมเขียน "หน้า i/n" ซึ่งผิด** เพราะ i คือลำดับการ์ดไม่ใช่แผ่นกระดาษ เปลี่ยนเป็น "ส่วนที่ i จาก n · ส่วนนี้ยาว k แผ่น A4" โดยวัดความสูงจริงหลังเรนเดอร์ · เพิ่มแถบบอกจำนวนแผ่นรวมเหนือพรีวิว โดยต้องห่อแถบกับพรีวิวไว้ใน .rightcol ด้วยกัน เพราะ main เป็นกริดสองคอลัมน์ ถ้าวางแถบเป็นลูกของ main ตรง ๆ แถบจะไปกินช่องคอลัมน์ขวาแล้วดันพรีวิวตกลงไปแถวล่างใต้แผงซ้าย (เจอบั๊กนี้จริง) · เปลี่ยนเป็นสีแดงเมื่อเกิน 10 แผ่น พร้อมแนะวิธีย่อ (ติ๊กเฉพาะที่เกินเกณฑ์ ปิดกราฟ ปิดตาราง) — ตัวอย่าง POMS+DSPOT ลดจาก 45 แผ่นเหลือ 10 แผ่นเมื่อติ๊กเฉพาะที่เกินเกณฑ์ · 🐛 **แถบท้ายกระดาษหลุดไปอยู่แผ่นใหม่ตามลำพัง** (ผู้ใช้ส่งภาพ 9 ก.ย. 69: ช่องว่างขาวยาวแล้วมีแต่แถบท้ายกระดาษ) เพราะบล็อกก่อนหน้าเป็น `break-inside: avoid` เมื่อที่เหลือไม่พอจึงถูกดันทั้งก้อนไปแผ่นใหม่ แล้วดัน `.ft` ตามไปด้วย — แก้ด้วย `.ft { page-break-before: avoid; break-before: avoid }` + ลด margin-top เหลือ 8px และฟอนต์ 9px ให้เกาะเนื้อหาก่อนหน้าเสมอ (จำลองการตัดหน้าในเบราว์เซอร์ยืนยันว่าไม่เหลือแถบลำพังทั้งสองส่วน)
  - **อธิบายช่วงราคาและค่ากลาง** หัวคอลัมน์เป็น "เงินลงทุน ต่ำ–สูง (บาท)" บรรทัดเทาใต้ช่วงราคาเขียนว่า "ค่ากลางที่ใช้คำนวณ" พร้อมคำอธิบายใต้ตารางว่าค่ากลางคือตัวเลขเดียวที่ใช้คิดคืนทุน NPV IRR LCOE และ ROI ในแถวนั้น และถ้ามีราคาจริงให้กรอกช่อง บาท/kWp แทน · รายงานพิมพ์ใช้หัวคอลัมน์ "เงินลงทุน ค่ากลาง บาท" พร้อมหมายเหตุเดียวกัน
  - **อัตราส่วนสมรรถนะเริ่มต้น 0.75** (เดิม 0.80) ทั้งใน `solarSizing`, `solarOption`, ช่องกรอกในหน้าออกแบบ และหน้ารายงาน · เปลี่ยนป้ายช่องจาก "PR เริ่มต้น" เป็น "อัตราส่วนสมรรถนะ" พร้อม tooltip อธิบาย · บรรทัดสรุปบอกทั้งค่าตั้งต้นและค่าที่ใช้จริงหลังลดตามอุณหภูมิแผง
  - **หน้าต่างแหล่งที่มาของข้อมูล** ปุ่มอยู่ในแผงซ้าย กดแล้วเปิดเป็น `<dialog>` โมดัล (ปิดด้วยปุ่ม คลิกนอกกรอบ หรือ Esc) · `REFERENCES` 22 รายการใน `sensor-catalog.js` แบ่ง 4 กลุ่ม (กฎหมาย · เอกสารวิชาการ · ชุดข้อมูล · เครื่องมือพัฒนา) รูปแบบ `ผู้แต่ง. (ปี). ชื่อเรื่อง. แหล่งที่มา.` + ลิงก์ + บรรทัด "ใช้ใน:" · ปิดท้ายด้วยชื่อผู้พัฒนา ดร.พีระพงษ์ ภัทรพันธุ์ชัย · สร้างครั้งเดียวตอนโหลดหน้าเพราะเนื้อหาไม่ขึ้นกับผลการออกแบบ · รายการอุปกรณ์ (BOQ) หุบเป็นค่าเริ่มต้น
  - **วิธีได้มาของค่า** `MEASURE_MODES` 4 ระดับ + `measureOf(param)` + `measureTag(param)` — `online` วัดต่อเนื่อง · `surrogate` ออนไลน์แบบค่าสหสัมพันธ์ต้องสอบเทียบกับแล็บ (COD, TSS, MLSS, NH4N) · `analyzer` เครื่องวิเคราะห์ใช้รีเอเจนต์ได้ค่าเป็นรอบ (TN, TP) · **`lab` ต้องส่งห้องปฏิบัติการแล้วกรอกผล (BOD เพราะต้องบ่ม 5 วัน, FOG)** · แสดงเป็นป้ายในรายการเลือกกราฟ ตาราง BOQ รายงานพิมพ์ และตารางสรุปในการ์ด 2 · เหตุผลอ้างข้อ 3 ของประกาศมาตรฐานน้ำทิ้งที่ให้ตรวจตาม Standard Methods
  - **การ์ด 4 จัดใหม่** ตัวเลือกดูกราฟ/เหตุการณ์ยกขึ้นบนสุดและอยู่นิ่ง · กราฟกับคอนโซล payload อยู่คู่กันใน `.simgrid` · **แอนิเมชันสายส่งย้ายไปอยู่ใน `<details>` ปิดไว้** เพราะเดิมขยับตลอดเวลาจนกดเลือกยาก (ยืนยันแล้วว่าตัวเลือกไม่ขยับอีก)
  - **ช่องพิกัดรวมเป็นช่องเดียว** `#sLatLng` วางจาก Google Maps ได้ทั้ง `13.7563, 100.5018` และลิงก์ที่มี `@lat,lng` · `parseLatLng()` ตรวจช่วง ±90/±180 และขึ้นขอบแดงเมื่อรูปแบบผิด · ช่อง `#sLat`/`#sLng` เดิมกลายเป็น hidden เพื่อไม่ให้ querystring และโค้ดเดิมพัง
  - **ปุ่ม ↺ คืนค่าโรงจริง** ในหัวการ์ด 2 คืนประเภทระบบ ความสามารถ น้ำเข้าจริง พิกัด ชื่อ ตามทะเบียน DSPOT และล้าง `state.qty`/`state.event`/`state.opt` · ปิดปุ่มเมื่อยังไม่ได้เลือกโรงจริง
  - เทสต์รวม 49 ตัว
  - เทสต์รวม 47 ตัว (`test/solar-finance.test.mjs` ใหม่ + `pages-syntax` + `sensor-catalog` + `envi-core`) · เปลี่ยนคำ "ชนิดระบบ" เป็น "ประเภทของระบบ" ทั้งระบบ
- ⬜ ยังไม่ได้นำเข้า: การใช้ที่ดิน LDD รายจังหวัด (**ข้ามไปก่อนตามที่ผู้ใช้เลือก 3 ก.ย. 69 — ติดขนาด DB Free**; ลิงก์ zip รายจังหวัดอยู่ `data/raw/ldd_links.json`) · ชั้นคุณภาพลุ่มน้ำ (ไม่มีบน ArcGIS ของ DWR)
- ⬜ ยังไม่มี key: NASA FIRMS (สำรองจุดความร้อน) · TMD uid/ukey ชุดเก่า (สถานีตรวจวัดจริง)
- ⚠️ `upsertStations()` อ่าน id กลับด้วย select ที่ PostgREST จำกัด 1,000 แถว — พอสำหรับ Air4Thai/TMD แต่ถ้าแหล่งไหน >1,000 สถานีและต้องเก็บ observations ต้องแบ่ง `.range()`

## บทเรียนจากการติดตั้งจริง (3 ก.ย. 2569 — อย่าทำซ้ำ)
- **revoke execute จาก public อย่างเดียวไม่พอ** — Supabase ตั้ง default privileges ให้ anon/authenticated ได้ EXECUTE โดยตรง ต้อง `revoke … from public, anon, authenticated, service_role` แล้ว grant กลับ (เจอ: anon เรียก purge_old_observations() ได้)
- **air4thai.pcd.go.th ส่ง TLS chain ผิด** (leaf Let's Encrypt YR1 แต่แนบ intermediate ของ Sectigo) → Deno ตอบ `UnknownIssuer` · แก้ด้วย `Deno.createHttpClient({ caCerts: [YR1] })` ในฟังก์ชัน · YR1 หมดอายุ 2 ก.ย. 2571 ต้องเปลี่ยนก่อนถึงวันนั้น
- **GISTDA MapServer ไม่รองรับ pagination** (`resultOffset`/`resultRecordCount` = error 400) → แบ่งหน้าด้วย `where FID > N` · geometry เป็น multipoint
- **DGR `?Page=N` คืน N×1000 แถวเริ่มที่ offset (N-1)×1000** (Page=60 = 58,723 แถว) ไม่มีพารามิเตอร์ขนาดหน้า → ใช้ลำดับหน้า 1,2,4,8,16,32,64 ทีละ 10k แถวต่อรอบ · แถวอยู่ใน `result`
- **Dashboard ควบคุมด้วยเบราว์เซอร์อัตโนมัติ**: ช่อง Secrets เป็น React input พิมพ์ด้วย synthetic key ไม่ติด ต้องใช้ native value setter + `input` event · สวิตช์ Verify JWT ก็ต้อง `el.click()` ผ่าน JS · Monaco ของ SQL Editor: `monaco.editor.getModels()[0].setValue()` แล้ว fetch ไฟล์จาก raw.githubusercontent.com ด้วย **commit SHA** (branch URL แคช ~5 นาที)
- **Nominatim ส่งคำนำหน้าเต็ม** ("อำเภอเมืองปทุมธานี") → strip ก่อนเติม อ./จ.
- **Dashboard ของ Supabase ไม่เรนเดอร์เมื่อแผงเบราว์เซอร์ของแอปถูกซ่อน** (`document.hidden === true` → หน้าค้าง "Supabase" ไม่มี Monaco แม้รอ 5 นาที; override `document.hidden` ก็ไม่ช่วย) — ต้องให้ผู้ใช้เปิดแผง Browser ไว้ตอนสั่งงาน Dashboard · หน้าเว็บของเราเอง (localhost/Pages) รัน JS ได้ปกติแม้แผงซ่อน · ถ้า Dashboard ใช้ไม่ได้ ให้ปล่อย cron ยิง handler แล้วตรวจผ่าน REST anon (`stations`, `v_station_latest`) แทน `ingest_runs`
- **`vacuum full` ต้องรันทีละคำสั่งใน SQL Editor** (นอก transaction) หลัง migration ที่ลบ/ย้ายข้อมูลก้อนใหญ่ ไม่งั้นขนาด DB ไม่ลด · `pg_database_size` เป็นตัวเลขที่ Supabase ใช้คิดโควตา (ไม่ใช่ผลรวม pg_total_relation_size)
